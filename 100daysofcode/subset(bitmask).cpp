#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	char ch[10];
	cin >> ch;

	int l = ch.length();
	l = l - 1;
	int lastbit, j;
	for (int i = 1; i <= l; i++)
	{
		j = i;
		while (j > 0)
		{
			lastbit = (j & 1)
			          if ()
				          j >> 1;
		}
	}



	return 0;
}

